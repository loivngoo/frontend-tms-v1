{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    // "start": "live-server --port=3000 --open=`./index.html`",
    "heroku-postbuild": "live-server --port=3000 --open=`./index.html`",
    "heroku-prebuild": "echo This runs before Heroku installs dependencies.",
    "heroku-cleanup": "echo This runs after Heroku prunes and caches dependencies."
  
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.2",
    "nodemon": "^2.0.16"
  }
}